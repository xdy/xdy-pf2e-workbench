declare class EnvironmentCanvasGroupPF2e extends EnvironmentCanvasGroup {
    /** Ensure prepared values are passed in initialization (upstream retrieves source values). */
    initialize(config?: EnvironmentInitializeConfig): void;
}
export { EnvironmentCanvasGroupPF2e };
